[project]
name = "bulletproof-plasma-package"
authors = ["Plasma Research Team"]
description = "A decentralized learning progression system with plasma network-inspired architecture"
telemetry = false
requirements = []

[contracts.plasma-milestone-tracker]
path = "contracts/plasma-milestone-tracker.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = true
trusted_sender = false
trusted_caller = false
callee_filter = false
